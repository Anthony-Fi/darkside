# Environment configuration for Aurora Night map server
# Copy this file to .env and fill in your values.

# OpenWeatherMap API key (required for cloud tiles via /owm_clouds)
OWM_API_KEY=
# Which OWM layer to use. 'clouds' works for free keys; 'clouds_new' may require higher plan.
OWM_LAYER=clouds

# Optional: server port (defaults to 3000)
# PORT=3000
